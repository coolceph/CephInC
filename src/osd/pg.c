#include "pg.h"

int cceph_pg_new(
        cceph_pg** pg,
        int64_t    log_id) {

    return CCEPH_OK;
}

int cceph_pg_free(
        cceph_pg** node,
        int64_t    log_id) {

    return CCEPH_OK;
}
